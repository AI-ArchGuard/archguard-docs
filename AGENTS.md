# ArchGuard 工作约定

## 工作原则

1. 修改前先阅读目标仓库的 README、AGENTS、相关需求、设计和 ADR。
2. 先检查现有代码、测试和工作树；不得覆盖用户已有修改。
3. 一次任务只解决一个明确问题，不顺带大规模重构。
4. 先给出实施计划、影响范围和风险，再修改文件。
5. 优先复用现有抽象；新增依赖必须说明理由、许可证和替代方案。
6. 禁止提交密钥、Token、密码、真实客户源码或个人数据。
7. 禁止为了让测试通过而删除测试、降低断言或吞掉异常。
8. 修改后运行最小相关测试，再运行仓库完整质量检查。
9. 未实际运行的验证必须明确标注，不得声称已经通过。
10. 跨仓库变更必须列出兼容顺序、版本和回滚方案。

## 架构限制

- `archguard-platform` 初期保持模块化单体，不因“展示微服务”而拆分。
- 领域模块不得直接访问其他模块的数据库表或内部实现。
- 领域层不得依赖 Spring Web、数据库驱动或外部模型 SDK。
- Scanner 输出稳定的版本化契约，不依赖 Platform 内部类。
- MCP 工具默认只读、最小权限；写操作必须显式审批。
- LLM 输出是不可信输入，必须做 Schema、权限和业务校验。
- 确定性规则是事实基础，LLM 负责解释、归纳与低风险建议。
- 数据库、Redis 和内部服务端口不得暴露到公网。

## 代码要求

- Java：构造器注入；禁止字段注入；公共边界使用明确 DTO；事务边界放在应用层。
- Go：显式错误处理；所有外部调用接受 `context.Context`；避免 Goroutine 泄漏。
- API：错误结构统一；分页、幂等、版本和兼容策略明确。
- 数据库：Schema 变更必须使用迁移脚本；已发布迁移不得修改。
- 日志：结构化输出，包含 traceId；禁止记录凭据和完整源码。
- 测试：修复缺陷必须先增加能够复现问题的测试。

## 仓库专属边界

- 本仓库是产品、需求、总体架构、ADR、工程规范、运行手册和交付报告的事实来源。
- 不承载 Platform、Scanner、MCP Gateway 或部署制品的业务实现。
- 规范性文档应区分当前事实、目标状态、建议和待决策项，不把未运行的能力描述为已完成。
- ADR 使用编号、状态和替代关系；已接受 ADR 不直接改写结论，需新增 ADR 取代。
- 文档链接优先使用相对路径，示例必须使用合成数据和占位凭据。

## 验证命令

- 当前基线：`git diff --check`、Markdown 相对链接检查、YAML 解析和常见 Secret 模式扫描。
- 修改目录或入口时，确认 `README.md` 能导航到所有规范、模板和报告。
- 修改跨仓库规范时，逐仓库核对 README、AGENTS 和契约描述不存在冲突。

## 输出格式

每次完成任务时报告：完成内容、修改文件、关键选择、测试结果、已知限制和下一步。
